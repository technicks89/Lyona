# Selects the generated lyona theme. The theme itself is written into
# /usr/share/plymouth/themes/lyona by scripts/lyona-plymouth-theme at ISO build
# time, so it is not checked in: it follows config/themes.toml instead.
[Daemon]
Theme=lyona
ShowDelay=0
